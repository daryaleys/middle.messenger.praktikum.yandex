{{#> profile-page}}
    {{#if isDataEditMode}}
        {{> profile-edit-form fields=profileData.mainFields}}
    {{else if isPasswordEditMode}}
        {{> profile-edit-form fields=profileData.passwordFields}}
    {{else}}
        {{> profile-details}}
        {{> profile-actions}}
    {{/if}}
{{/profile-page}}
