<div
    class="inline-form-field"
    data-validation-field
    data-validation-state="{{#if isInvalid}}invalid{{else}}valid{{/if}}"
>
    <label class="inline-form-field__label" for="{{inputId}}">{{label}}</label>
    {{{Input
        className="inline-form-field__input"
        id=inputId
        type=type
        name=name
        value=inputValue
        validationRule=validationRule
        isInvalid=isInvalid
        describedBy=errorId
    }}}
    <span
        class="inline-form-field__error"
        id="{{errorId}}"
        data-validation-error
        {{#unless errorMessage}}hidden{{/unless}}
    >{{errorMessage}}</span>
</div>
