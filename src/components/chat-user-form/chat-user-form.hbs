<form class="chat-user-form" action="#" method="post" novalidate>
    <h2 class="chat-user-form__title">{{title}}</h2>

    {{{FormField
        label="Логин"
        name="login"
        type="text"
        validationRule="login"
        value=(fieldValue formValues "login")
    }}}

    {{#if error}}
        <p class="chat-user-form__error" role="alert">{{error}}</p>
    {{/if}}

    {{{SubmitButton
        text=submitText
        loadingText="Сохраняем..."
        className="chat-user-form__submit"
        disabled=isLoading
        isLoading=isLoading
    }}}
</form>
