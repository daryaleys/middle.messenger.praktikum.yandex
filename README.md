# Messenger

Учебный проект мессенджера для курса Яндекс Практикума. В проекте реализованы страницы авторизации, регистрации, профиля и чатов, компонентная архитектура на Handlebars/TypeScript, клиентский роутинг, валидация форм, стор приложения и работа с API.

В проекте использован стандартный дизайн от Практикума:
[макет в Figma](https://www.figma.com/design/jF5fFFzgGOxQeB4CmKWTiE/Chat_external_link?node-id=0-1&p=f&t=YOQ4orgNIn5cl1n7-0).

## Демо

Проект на Netlify: https://wonderful-pasca-c952b9.netlify.app

## Возможности

- Авторизация, регистрация и выход из аккаунта.
- Защищенные и публичные роуты с проверкой текущего пользователя.
- Просмотр профиля, редактирование данных, смена пароля и аватара.
- Загрузка списка чатов с состояниями загрузки и ошибки.
- Создание нового чата.
- Выбор активного чата и отображение участников чата.
- Добавление и удаление пользователей из выбранного чата.
- Переиспользуемые UI-компоненты: формы, поля ввода, кнопки, модалки, дропдауны, элементы списка чатов.
- Валидация форм на клиенте.

## Установка

```bash
npm install
```

## Команды

```bash
npm run dev
```

Запускает проект в режиме разработки. По умолчанию приложение доступно по адресу `http://localhost:3000`.

```bash
npm run start
```

Аналогично запускает Vite-сервер через `dotenv`.

```bash
npm run build
```

Собирает production-версию проекта в директорию `dist`.

```bash
npm run preview
```

Запускает локальный preview production-сборки.

```bash
npm run lint
```

Запускает Stylelint и ESLint.

## Страницы

После запуска проекта страницы можно посмотреть по следующим роутам:

- `/` — страница входа.
- `/sign-up` — страница регистрации.
- `/messenger` — страница чатов.
- `/settings` — профиль пользователя.
- `/settings/edit` — редактирование данных профиля.
- `/settings/password` — изменение пароля.
- `/404` — страница ошибки 404.
- `/500` — страница ошибки 500.

## API

Приложение использует API Практикума: `https://ya-praktikum.tech/api/v2/swagger/#/`.

Подключены запросы:

- auth: вход, регистрация, получение текущего пользователя, выход;
- user: редактирование профиля, смена пароля, загрузка аватара, поиск пользователя;
- chats: получение списка чатов, создание чата, получение пользователей чата, добавление и удаление пользователей.

## Стек

- TypeScript
- Vite
- Handlebars
- CSS
- ESLint
- Stylelint
